[project]
name = "campulse-backend"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv",
    "fastapi[standard]",
    "uvicorn",
    "logfire[fastapi]",
]

[tool.uv]
package = false

[tool.logfire]
ignore_no_config = true