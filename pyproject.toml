[project]
name = "campulse-backend"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]",
    "logfire[fastapi,sqlalchemy]",
    "sqlmodel",
    "psycopg2-binary",
]

[tool.uv]
package = false