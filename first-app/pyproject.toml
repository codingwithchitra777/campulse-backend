[project]
name = "first-app"
version = "1.0.0"
requires-python = "==3.12"
dependencies = [
    "fastapi[standard]==0.111.0",
    "logfire[fastapi]==4.37.0",
]

#[tool.fastapi]
#entrypoint = "main:app"
[tool.uv]
package = false